export class Book{
    id: number;
    title: String;
    author: String;

    constructor(id:number, title:String, autor:String){
        this.id = id,
        this.title = title;
        this.author = autor;
    }
}